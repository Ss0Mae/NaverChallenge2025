# ✅ Day18-19: 나만의 체크포인트

## 📚 1단계: 기본 서버 및 클라이언트 구축

- [x] **TCP 서버 골격 생성 (`server.js`)**
    - [x] `net.createServer()`를 사용하여 기본 TCP 서버 구현
    - [x] 2025 포트에서 리스닝 시작
    - [x] 새로운 클라이언트 접속 시 `connection` 이벤트 처리
- [x] **메시지 파서 구현 (`parser.js`)**
    - [x] `Content-Length` 헤더를 기반으로 전체 메시지를 수신하는 로직 구현
    - [x] 수신된 데이터를 JSON으로 파싱하는 기능 구현
    - [x] 불완전한 데이터(chunk)를 임시 버퍼에 저장하고 관리하는 기능 구현
- [x] **기본 클라이언트 골격 생성 (`client.js`)**
    - [x]  `net.createConnection()`으로 서버에 접속
    - [x] `readline`을 사용하여 사용자 입력 처리
    - [x] 서버로부터 받은 데이터를 파싱하여 콘솔에 출력

---

## 🏢 2단계: 핵심 기능 구현 (고객 클라이언트 중심)

- [x] **세션 관리자 구현 (`SessionManager.js`)**
    - [x] 클라이언트 소켓별로 고유 ID를 부여하고 관리
    - [x] 로그인 상태, `campId`, 소속 그룹 등 클라이언트 정보 저장
- [x] **`login` / `logout` 기능 구현**
    - [x] `campId` 유효성 검사 (S001 ~ S256)
    - [x] 쇼핑 그룹 자동 배정 로직 구현 (최대 4명)
    - [x] 로그인 성공 시 그룹 번호 응답
    - [x] 로그아웃 시 그룹에서 제거 및 다른 그룹원에게 알림 전송
    - [x] 비정상 종료(`close` 이벤트) 시 로그아웃 처리
- [x] **상품 관리자 구현 (`ProductManager.js`)**
    - [x] 상품 목록(id, 이름, 재고)을 메모리에 저장 및 관리
    - [x] 15개 이상의 기본 상품 데이터 생성
- [x] **`catalog` / `buy` 기능 구현**
    - [x] `catalog` 요청 시 전체 상품 목록 응답
    - [x] `buy` 요청 시 재고 확인 및 차감 로직 구현
    - [x] 재고 부족 시 에러 응답

---

## 💬 3단계: 채팅 및 브로드캐스트 기능 구현

- [x] **채팅 관리자 구현 (`ChatManager.js`)**
    - [x] `snapchat` 요청 시 그룹 채팅방 생성 및 상태 관리 (활성화 여부, `maxCount`)
    - [x] `finish` 요청 시 채팅방 비활성화
- [ ] **그룹 채팅 (유니캐스트) 구현**
    - [ ] `message` 요청 시 같은 그룹에 속한 클라이언트에게만 메시지 전송
    - [ ] `maxCount` 초과 시 메시지 전송 차단
- [x] **UDP 브로드캐스트 구현**
    - [x] `dgram.createSocket('udp4')`를 사용하여 UDP 소켓 생성
    - [x] 고객 클라이언트는 UDP 포트를 리스닝하여 브로드캐스트 메시지 수신
    - [x] MD 클라이언트의 `broadcast` 요청 시, 서버가 모든 고객에게 UDP 메시지 전송

---

## 👨‍💼 4단계: MD 클라이언트 기능 구현

- [x] **MD 클라이언트 로직 구현 (`mdClient.js`)**
    - [x] 별도의 MD 클라이언트 파일 생성
    - [x] `auth` 요청 시 보안 토큰 전송 로직 구현
- [x] **서버 측 MD 인증 및 상품 관리 기능 구현**
    - [x] `auth` 요청에 대한 토큰 검증 로직 구현
    - [x] MD 클라이언트 연결은 단 하나만 유지하도록 처리
    - [x] `add` 요청 시 `ProductManager`를 통해 상품 추가 또는 재고 업데이트

---

## 🧪 5단계: 테스트 및 리팩토링

- [ ] **기능별 통합 테스트**
    - [ ] 여러 고객 클라이언트와 MD 클라이언트를 동시에 실행하여 기능 정상 동작 확인
    - [ ] 동시 접속, 그룹 채팅, 재고 변경 등 시나리오 테스트
- [ ] **코드 리팩토링**
    - [ ] 각 모듈의 책임(SRP)이 잘 분리되었는지 검토
    - [ ] 중복 코드 제거 및 가독성 향상
