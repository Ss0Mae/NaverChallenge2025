# 📝 Day 8: 나만의 체크포인트

## ✅ 미션 1: 함수형 불변 데이터 구조 구현

- [X] **환경 설정**
    - [X] `package.json` 초기화 (`npm init -y`)
    - [X] 테스트 라이브러리(Jest 등) 설치 및 설정

- [X] **불변 연결 리스트 (ImmutableLinkedList) 구현**
    - [X] **Node** 클래스 또는 프로토타입 정의 (값, 다음 노드 참조)
    - [X] **ImmutableLinkedList** 클래스 또는 프로토타입 정의 (head 참조)
    - [X] `append(value)`: 새로운 리스트 반환
    - [X] `item(at)`: 특정 인덱스의 값 찾기 (재귀 활용)
    - [X] `insert(value, at)`: 새로운 리스트 반환 (재귀 활용)
    - [X] `remove(at)`: 새로운 리스트 반환 (재귀 활용)
    - [X] `clear()`: 비어있는 새로운 리스트 반환
    - [X] 각 기능에 대한 단위 테스트 작성

- [X] **불변 해시맵 (ImmutableHashMap) 구현**
    - [X] **ImmutableHashMap** 클래스 또는 프로토타입 정의 (buckets 참조)
    - [X] 해시 함수(`_hash(key)`) 구현
    - [X] `put(key, value)`: 새로운 해시맵 반환 (연결 리스트 활용)
    - [X] `get(key)`: 키에 해당하는 값 찾기
    - [X] `contains(key)`: 키 존재 여부 확인
    - [X] `remove(key)`: 새로운 해시맵 반환
    - [X] `keys()`: 모든 키를 배열로 반환
    - [X] 각 기능에 대한 단위 테스트 작성

## ✅ 미션 2: 도서 관리 프로그램 `CampLibrary` 구현

- [X] **기본 구조 설계**
    - [X] **Book** 데이터 타입(클래스/객체) 정의 (제목, 저자, ISBN 등)
    - [X] **CampLibrary** 클래스 또는 프로토타입 정의 (도서 목록을 담을 불변 데이터 구조 활용)

- [X] **핵심 기능 구현**
    - [X] `add(book)`: 새로운 `CampLibrary` 인스턴스 반환
    - [X] `rent(book, person)`: 대여 정보 포함한 새로운 `CampLibrary` 인스턴스 반환
    - [X] `return(book, person)`: 반납 처리된 새로운 `CampLibrary` 인스턴스 반환
    - [X] `totalRent()`: 현재 대여 정보를 담은 `ImmutableHashMap` 반환
    - [X] 각 기능에 대한 단위 테스트 작성

- [X] **조회 및 분석 기능 구현**
    - [X] `findBy(author)`: 저자 검색 결과 배열 반환
    - [X] `sortByDate()`: 발행년도 정렬 결과 배열 반환
    - [X] `top10rent()`: 대여 횟수 Top 10 배열 반환
    - [X] 각 기능에 대한 단위 테스트 작성

- [ ] **고차 함수 기능 구현**
    - [ ] `map(lambda)`: 변환된 `ImmutableLinkedList` 반환
    - [ ] `filter(lambda)`: 필터링된 `ImmutableLinkedList` 반환
    - [ ] `display(lambda)`: 순회 기능 구현
    - [ ] 각 기능에 대한 단위 테스트 작성

## 🧐 학습 및 회고

- [ ] 함수형 프로그래밍의 핵심(불변성, 순수함수) 개념 정리
- [ ] 불변 데이터 구조 구현 시 재귀적 사고방식 적용 경험 정리
- [ ] 기존 명령형 프로그래밍 방식과 함수형 방식의 차이점 비교 분석
- [ ] 시간/공간 복잡도 분석 및 트레이드오프에 대한 고민 정리
