# Day 2: 나만의 체크포인트

## 🐧 첫 번째 미션: Linux/Unix 환경과 친숙해지기

- [x] VMware에 Ubuntu 24.04 Desktop 설치 완료
- [x] `openssh-server` 설치 및 SSH 서비스 활성화
- [x] 방화벽(ufw) 설정 및 SSH 포트 허용
- [x] VM IP 주소 확인
- [x] `adduser`를 이용한 신규 사용자 계정 생성
- [x] 로컬에서 SSH로 신규 계정 원격 접속 성공
- [x] `/monitoring` 디렉토리 생성
- [x] `chmod 764 /monitoring` 권한 변경 및 `ls -l`로 확인 (스크린샷)
- [x] `timedatectl`을 이용해 'Asia/Seoul' 시간대 설정
- [x] `date` 명령어로 변경된 시간 확인 (스크린샷)
- [x] `nvm` 설치
- [x] `nvm`을 이용해 Node.js 최신 LTS 버전 설치
- [x] `node -v`로 버전 확인
- [x] `scp` 또는 `sftp`로 Day1 과제 파일 VM에 복사
- [x] VM에서 Day1 과제 Node.js 스크립트 실행 성공
- [x] `README.md`에 과정 상세 기록 완료
- [x] `학습정리.md`에 학습 내용 정리 완료
- [x] 모든 산출물 Gist에 업로드 및 링크 확인

## 🚀 두 번째 미션: 쉘 스크립트와 sLLM 자동화

- [x] `ollama` 설치 및 시스템 재부팅 시 자동 실행 설정 완료
- [x] `ollama` 외부 접속 설정 및 방화벽 확인
- [x] `gemma:1b` 모델 실행 및 `curl` 외부 접속 테스트 성공
- [x] `dmesg` 로그 수집 쉘 스크립트(`collect_logs.sh`) 작성
- [x] `crontab`에 로그 수집 스크립트 등록 (1분 주기)
- [x] 로그 분석 및 `ollama` 연동, Slack 알림 쉘 스크립트(`analyze_and_notify.sh`) 작성
- [x] `crontab`에 분석/알림 스크립트 등록 (1시간 주기)
- [x] Slack 웹훅으로 테스트 알림 전송 성공
- [x] 알림 내용 `/monitoring` 폴더에 로그 파일로 저장 기능 구현 확인
- [x] `README.md`에 두 번째 미션 과정 상세 기록 완료
- [x] `학습정리.md`에 쉘 스크립트, `crontab`, `ollama` 관련 내용 정리 완료
