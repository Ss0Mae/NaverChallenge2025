class Stone {
    /**
     * @param {'WHITE' | 'BLUE' | 'RED' | 'GREEN' | 'ORANGE' | 'VIOLET'} type - 스톤의 색깔
     */
    constructor(type) {
        this.type = type;
    }
}

module.exports = Stone;
