# Day 3: 나만의 체크포인트

## 1. CS 지식 학습 목표

- [x] **파서(Parser)와 렉서(Lexer/Tokenizer)의 역할 이해:**
    - [x] 소스 코드를 어떻게 의미 있는 조각(토큰)으로 나누는지 학습한다.
    - [x] 토큰들을 바탕으로 어떻게 문법적 구조(트리)를 만드는지 원리를 이해한다.
- [x] **트리(Tree) 자료구조 학습:**
    - [x] 부모-자식 관계, 노드, 엣지, 루트, 리프 등 트리의 기본 용어를 익힌다.
    - [x] 트리 순회(Traversal) 방식인 깊이 우선 탐색(DFS)과 너비 우선 탐색(BFS)의 차이를 이해한다.
- [x] **재귀(Recursion)의 개념 및 활용 학습:**
    - [x] 재귀 함수의 기본 구조(Base Case, Recursive Step)를 이해한다.
    - [x] 중첩된 데이터 구조(트리)를 탐색하고 처리하는 데 재귀가 왜 효과적인지 학습한다.

## 2. 구현 로드맵

### 1단계: 설계 및 환경 준비
- [x] `README.md`에 구현 계획 상세히 작성하기
- [x] 내부 데이터 구조 최종 확정하기 (특히 자식 노드와 텍스트 값 처리 방식)
- [x] `xml-parser.js` 파일 및 기본 함수 구조 생성

### 2단계: 핵심 파서 구현
- [x] **토크나이저(Tokenizer) 구현:**
    - [x] 여는/닫는 태그 토큰 분리 기능
    - [x] 속성(attribute) 토큰 분리 기능
    - [x] 텍스트(value) 토큰 분리 기능
    - [x] 자체 종료 태그 (`<BR/>`) 처리 기능
    - [x] 프롤로그(`<?xml ...>`) 및 주석(`<!... >`) 처리 기능
- [x] **파서 및 트리 빌더(Parser & Tree Builder) 구현:**
    - [x] 토큰 스트림을 받아 재귀적으로 트리 구조를 만드는 메인 파서 함수 구현
    - [x] 중첩된 태그를 처리하여 `children` 배열에 자식 노드를 추가하는 로직 구현

### 3단계: 데이터 활용 함수 구현
- [x] `displayJSON()` 함수 구현
- [x] `elementByAttribute()` 함수 구현
- [x] `reportByClass()` 함수 구현
- [x] `findXPath()` 함수 구현

### 4단계: 테스트 및 리팩토링
- [x] 제공된 3가지 XML 샘플(HTML, Android, Plist) 모두 정상적으로 파싱되는지 테스트
- [x] 함수가 하나의 역할만 하도록 분리하고, 변수명을 명확하게 하는 등 코드 가독성 개선
- [x] `학습정리.md`에 학습한 CS 지식과 경험 상세히 기록